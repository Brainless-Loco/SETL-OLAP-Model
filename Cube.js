class Cube {
  constructor(sub,pred,obj){
    this.sub = sub;
    this.pred = pred;
    this.obj = obj;
  }
  hasComponent = [
    (
      type,
      dimensionType,
      dimensionid
    )
  ] //array of pair<hasComponentType,dimensionType,dimensionId>
}
  
